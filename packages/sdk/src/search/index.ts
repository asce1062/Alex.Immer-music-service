/**
 * Search Module
 *
 * Client-side search functionality for albums, tracks, and tracker modules
 */

export { SearchManager } from './SearchManager';
export type { SearchManagerConfig } from './SearchManager';
